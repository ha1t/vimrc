SDO_Model_Property::isContainment --
プロパティが包含関係を定義しているかどうかを調べる

  bool SDO_Model_Property::isContainment()~

この関数は、 実験的 なものです。この関数の動作・
名前・その他ドキュメントに書かれている事項は、予告なく、将来的な
PHP のリリースにおいて変更される可能性があります。
この関数は自己責任で使用してください。

このプロパティが包含関係を表すものかどうかを調べます。
包含関係を定義したものである場合に TRUE、
単なる参照である場合に FALSE を返します。

なし。

プロパティが包含関係を定義したものである場合に TRUE、
単なる参照である場合に FALSE を返します。

vim:ft=help:
