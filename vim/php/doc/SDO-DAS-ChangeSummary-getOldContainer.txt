SDO_DAS_ChangeSummary::getOldContainer -- 削除された SDO_DataObject
の削除前のコンテナを取得する

  SDO_DataObject SDO_DAS_ChangeSummary::getOldContainer(SDO_DataObject data_object)~

この関数は、 実験的 なものです。この関数の動作・
名前・その他ドキュメントに書かれている事項は、予告なく、将来的な
PHP のリリースにおいて変更される可能性があります。
この関数は自己責任で使用してください。

削除された SDO_DataObject の削除前のコンテナ (SDO_DataObject)
を取得します。

{data_object} 削除された SDO_DataObject
で、そのコンテナを調べたいもの。

削除された SDO_DataObject
の削除前のコンテナオブジェクトを返します。

vim:ft=help:
