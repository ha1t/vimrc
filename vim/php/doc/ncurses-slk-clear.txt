ncurses_slk_clear -- 画面からソフトラベルをクリアする

  bool ncurses_slk_clear()~

この関数は、 実験的 なものです。この関数の動作・
名前・その他ドキュメントに書かれている事項は、予告なく、将来的な
PHP のリリースにおいて変更される可能性があります。
この関数は自己責任で使用してください。

関数 |ncurses_slk_clear| は、
画面からソフトラベルキーを消去します。

エラー時に TRUE、 それ以外の場合に FALSE を返します。

vim:ft=help:
