stats_rand_gen_normal -- mean、av および標準偏差 sd (sd >= 0)
によって表される正規分布から無作為な値を生成する。
Method : Renames SNORM from TOMS as slightly modified by BWB to use RANF
instead of SUNIF.

  float stats_rand_gen_normal(float av, float sd)~

この関数は、
現在のところ詳細な情報はありません。引数のリストのみが
記述されています。

{av}

{sd}



vim:ft=help:
