atanh --
逆双曲線正接（アークハイパボリックタンジェント）

  float atanh(float arg)~

{arg} のアークハイパボリックタンジェントを返します。
つまり、ハイパボリックタンジェントが {arg}
となる値です。

この関数は Windows 環境にはまだ実装されていません。

{arg} 処理する引数。

{arg} のアークハイパボリックタンジェントを返します。

|tanh| |atan | |asinh| |acosh|

vim:ft=help:
