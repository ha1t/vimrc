ncurses_can_change_color --
端末の色を変更可能かどうか確認する

  bool ncurses_can_change_color()~

この関数は、 実験的 なものです。この関数の動作・
名前・その他ドキュメントに書かれている事項は、予告なく、将来的な
PHP のリリースにおいて変更される可能性があります。
この関数は自己責任で使用してください。

端末が色を扱えるかどうか、
そしてプログラムで色を変更できるかどうかを返します。

端末が色を扱え、かつプログラマがそれを変更できる場合に
TRUE、それ以外の場合に FALSE を返します。

vim:ft=help:
