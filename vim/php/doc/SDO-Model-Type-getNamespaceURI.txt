SDO_Model_Type::getNamespaceURI -- 型の名前空間 URI を取得する

  string SDO_Model_Type::getNamespaceURI()~

この関数は、 実験的 なものです。この関数の動作・
名前・その他ドキュメントに書かれている事項は、予告なく、将来的な
PHP のリリースにおいて変更される可能性があります。
この関数は自己責任で使用してください。

型の名前空間 URI を返します。型の名前および名前空間 URI
を組み合わせることで 型を特定します。

なし。

型の名前空間 URI を返します。

vim:ft=help:
