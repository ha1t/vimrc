textdomain -- デフォルトドメインを設定する

  string textdomain(string text_domain)~

この関数は、|gettext| がコールされた際に検索を
行うドメインを設定します。このドメインは、通常はアプリケーション名から
付けられます。

{text_domain} 新しいメッセージドメイン。NULL
を指定すると、現在の設定をそのまま
取得し、変更しません。

成功すると、この関数はドメインを変更した後に現在のメッセージドメインを
返します。

vim:ft=help:
