ncurses_flash -- 端末画面をフラッシュする(ビジュアルベル)

  bool ncurses_flash()~

この関数は、 実験的 なものです。この関数の動作・
名前・その他ドキュメントに書かれている事項は、予告なく、将来的な
PHP のリリースにおいて変更される可能性があります。
この関数は自己責任で使用してください。

画面をフラッシュします。
もしそれができなかった場合は、音声の警告(ベル)を送信します。

成功した場合に FALSE、そうでない場合に TRUE を返します。

|ncurses_beep|

vim:ft=help:
