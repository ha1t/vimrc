m_deletetrans -- MCVE_CONN
構造体から、指定したトランザクションを削除する

  bool m_deletetrans(resource conn, int identifier)~

この関数は、
現在のところ詳細な情報はありません。引数のリストのみが
記述されています。

{ conn}|m_initengine| が返す MCVE_CONN リソース。

{identifier}



vim:ft=help:
