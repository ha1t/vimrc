ZipArchive::close -- アクティブな
(オープンされた、あるいは新しく作成された)
アーカイブを閉じる

  bool ZipArchive::close()~

オープンされた、あるいは作成されたアーカイブを閉じ、
変更内容を保存します。このメソッドは、
スクリプトの最後で自動的にコールされます。

成功した場合に TRUE を、失敗した場合に FALSE
を返します。

vim:ft=help:
