connection_aborted --
クライアントとの接続が切断されているかどうかを調べる

  int connection_aborted()~

クライアントとの接続が切断されているかどうかを調べます。

クライアントとの接続が切断されている場合に
1、それ以外の場合に 0 を返します。

|connection_status| |ignore_user_abort| PHP
における接続処理についての説明は 接続処理

vim:ft=help:
