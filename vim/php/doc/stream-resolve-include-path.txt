stream_resolve_include_path -- fopen
に相対パスを指定してコールされたときに、
どのファイルをオープンするかを決める

  string stream_resolve_include_path(string filename [, resource context])~

この関数は、
現在のところ詳細な情報はありません。引数のリストのみが
記述されています。

vim:ft=help:
