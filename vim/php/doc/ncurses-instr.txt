ncurses_instr -- 端末画面から文字列を読み込む

  int ncurses_instr(string buffer)~

この関数は、 実験的 なものです。この関数の動作・
名前・その他ドキュメントに書かれている事項は、予告なく、将来的な
PHP のリリースにおいて変更される可能性があります。
この関数は自己責任で使用してください。

現在の位置から行末までを読み込み、その文字数を返します。

{buffer} 読み込んだ文字列。文字の属性は削除されます。

文字の数を返します。

vim:ft=help:
