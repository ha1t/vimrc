exp -- e の累乗を計算する

  float exp(float arg)~

e を {arg} 乗した値を返します。

'e' は自然対数の底で、およそ 2.718282 です。

{arg} 処理する引数。

'e' の {arg} 乗を返します。

|exp| の例

<?php >
  echo exp(12) . "\n";
  echo exp(5.7);
?>

上の例の出力は以下となります。

  1.6275E+005
  298.87



|log| |pow|

vim:ft=help:
