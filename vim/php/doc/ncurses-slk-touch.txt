ncurses_slk_touch -- ncurses_slk_noutrefresh
を実行する際に強制的に出力する

  int ncurses_slk_touch()~

この関数は、 実験的 なものです。この関数の動作・
名前・その他ドキュメントに書かれている事項は、予告なく、将来的な
PHP のリリースにおいて変更される可能性があります。
この関数は自己責任で使用してください。

次に |ncurses_slk_noutrefresh| が実行された際に
すべてのソフトラベルを強制的に出力するようにします。

vim:ft=help:
