printer_delete_pen -- ペンを削除する

  void printer_delete_pen(resource pen_handle)~

この関数は、選択したペンを削除します。使用例は
|printer_select_pen| を参照ください。

{pen_handle} {pen_handle} は、
有効なペンのハンドルである必要があります。

値を返しません。

vim:ft=help:
