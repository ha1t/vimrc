PDF_fill_textblock --
テキストブロックをさまざまなデータで塗りつぶす

  int PDF_fill_textblock(resource pdfdoc, int page, string blockname, string text, string optlist)~

テキストブロックを、そのプロパティに基づいたさまざまなデータで塗りつぶします。

この関数は、PDFlib Personalization Server (PPS)
でのみ使用可能です。

vim:ft=help:
